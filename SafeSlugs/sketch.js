function setup() {
	createCanvas(windowWidth, windowHeight);
	colorMode(HSB);
	background(341, 79, 69);
}

function draw() {

	fill(205,60,60);
	noStroke();
	rectMode(CENTER);
	rect(windowWidth/2,100,windowWidth/2, 100, 20, 20);

	textSize(32);
	fill(255);
	textAlign(CENTER);
	text("Safe Slugs", windowWidth/2, 100, )

}
